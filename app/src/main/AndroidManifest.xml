<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ananananzhuo.composecolumn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposeColumn">
        <activity
            android:name=".ToggleableActivity"
            android:exported="true"
            android:label="@string/title_activity_toggleable"
            android:theme="@style/Theme.ComposeColumn.NoActionBar" />
        <activity
            android:name=".RememberActivity"
            android:exported="true"
            android:label="@string/title_activity_remember"
            android:theme="@style/Theme.ComposeColumn.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ComposeColumn.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>